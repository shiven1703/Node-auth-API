{
  "name": "auth-microservice",
  "version": "1.0.0",
  "description": "Basic authentication microservice",
  "main": "/bin/www",
  "scripts": {
    "start": "node -r dotenv/config ./bin/www",
    "start-dev": "nodemon -r dotenv/config ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shivam Patel",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "ajv": "^8.11.0",
    "ajv-formats": "^2.1.1",
    "bcrypt": "^5.0.1",
    "config": "^3.3.7",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "mongoose": "^6.3.2"
  }
}
